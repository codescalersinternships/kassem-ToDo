apiVersion: v1
kind: Pod
metadata:
  name: frontend
  labels:
    app: front 
spec:
  containers:
  - name: frontend
    image: 0om4r/todo:script.1
    imagePullPolicy: Always
    ports:
    - containerPort: 8080
